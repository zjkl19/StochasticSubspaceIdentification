使用说明：

matlab版本：R2016a

使用前应先安装"GUI Layout Toolbox 2.2.1.mltbx"，安装方法：直接运行即可

1、原始数据文件应为ASCII文件，每1列数据为1个通道的时程数据，数据不含表头
2、基本步骤：计算、做稳定图，绘制振型图。
3、稳定图中：蓝色圈代表稳定点，叉号代表频率和振型稳定，米字号代表频率和阻尼比稳定，十字代表仅频率。

将来版本的开发打算：
1、面向对象程序结构设计的完善
2、复模态转实模态功能开发
3、多参考点计算结果合并
4、后处理的完善
5、界面完善
6、基于图论的聚类识别的实现

附I：SSIConfigFile的说明：
1、采样频率
2、经验参数：c （和结构模型大小和测点数有关，详见论文）
3、计算的投影矩阵最大阶数，即稳定图系统的最大阶数的0.5倍
4、特征频率
5、特征阻尼比
6、MAC限值

附：II
1、程序中ExtractRshape方法暂不可用。
2、基于索引的异常稳定点剔除的算法中（rmAbnormWD.m文件），距离值如何确定？M值如何确定？


历次版本更新说明：

v2.2
1、添加绘制平均正则化功率谱密度函数图象的子功能（详见根目录下plotANPSD以及getANPSD函数）。
2、添加SSIMatSave计算过程中的进度提示。
3、智能化输出最终的结果文件名以及智能化处理结果的写入。
4、添加临时测试用的testScript.m脚本。
5、初步实现基于图论的聚类分析。

v2.1
1、添加基于协方差驱动的随机子空间方法（SSI_Cov），原先仅有基于数据驱动的随机子空间方法（SSI_Date）。
从直观上来看，基于协方差驱动的随机子空间方法稳定点要多。
2、修改SSI_Class的架构，分离基于MSI（模态相似指数，详见章国稳论文）的虚假模态剔除办法。
3、细化SSI_Class功能模块，添加getStatusOfPoles以及getStablePoles方法。
4、优化SSI_Cluster、SSIMatSave以及Custom文件的代码。

v2.0 较大程度升级
1、添加基于索引的算法的异常稳定点剔除的子程序，函数名称为：rmAbnormWD，详见《数据挖掘》第二版 朱明 P258
2、添加列出计算中文件名功能的函数：getFileName
3、在一定程度上解决了高阶模态识别时，频率区间重叠范围较大的问题（每识别一个频率，设定1个标记，后来识别的频率必须大于该频率）。
4、引入MSI（模态相似系数）来剔除虚假模态，在本程序中，默认取    Wf=0.5; Wkesi=0.2; WMAC=0.3;df=0.15;dkesi=0.30;dMAC=0.20;MSItor，模态相似指数容差=1 
详细理论见：基于谱系聚类的随机子空间模态参数自动识别——章国稳
5、添加新功能：可以保存SSI法svd分解后，计算的1：n阶的模态。结果存入SSI mat文件，这可以为后来的计算节约大量时间。
6、添加“定制功能”：基于SSI mat文件，自动识别模态，并将识别结果存入对应的ASCII文件。其中的核心识别算法所对应的文件是identifyMode
7、强化容错功能：如果在某个频率区间内未找到cluster，即判定该阶模态没有被激励出来。

v1.5
1、新功能的添加：对稳定点之间的距离进行了核密度估计，根据估计的概率密度函数曲线的极值点，自动判断分割距离。

v1.41
1、添加了计算MAC（模态置信准则）的函数getMAC
2、SSICluster方法中，除了拾取稳定点中的频率以外，还拾取了阻尼比和振型。
3、添加了基于ansys模型的结构模态识别方法（有待完善）。

v1.4
1、高级功能中，添加基于层次聚类的SSI法模态自动识别（初步完成，还需要进一步完善）。

v1.3
1、投影矩阵进行svd分解前，进行了CVA加权。
2、添加“周海飞专用”SSI法自动求解结构频率的自定义功能。
3、优化ReSamFilter.m文件（提高ReSamFilter.m文件可读性）
4、修正ExtraPanelView.m中一个会导致全局错误的拼写错误。

v1.2
1、添加主程序界面（核心算法不变）。
2、扩充部分类方法功能。
3、优化程序注释。

v1.1

1、分割类文件，便于程序维护和合作开发
2、优化程序注释
3、优化“可变参数”函数的编写
4、强化后处理模块，添加模态标准化的成员方法
5、主程序独立出后处理模块SSIPostProcessing.m

v1.0 首个版本
